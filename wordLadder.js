function ladderLength(beginWord, endWord, wordList) {
  let wordBranches = [];
  let idx = beginWord.length;
  let word = beginWord;

  while (idx < beginWord.length) {
    for (let i = 0; i < 26; i += 1) {

    }

    idx += 1;
  }
}

console.log(ladderLength("hit", "cog", ["hot","dot","dog","lot","log"]));
